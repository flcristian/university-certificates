# Docker Registry Configuration
DCR=localhost
DCR_ORG=local
DCR_REPO=myapp
DCR_TAG=latest

# Project Configuration
COMPOSE_PROJECT_NAME=university-certificates

# User Configuration (use your host user ID and group ID)
USER_ID=1000
GROUP_ID=1000

# Environment Configuration
ENVIRONMENT=development

# API Configuration
API_PORT=8000

# Database Connection
DB_HOST=localhost
DB_NAME=university_certificates_db
DB_USER=certs
DB_PASSWORD=admin
DB_PORT=3308